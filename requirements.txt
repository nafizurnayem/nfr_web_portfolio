# Thin pointer so `pip install -r requirements.txt` keeps working from the
# repository root. The real list lives in backend/requirements.txt -- Vercel's
# Python builder reads the service root's file and cannot follow pip includes,
# so the pins must stay in a PLAIN list there.
-r backend/requirements.txt
