# The dependency list lives at the repository root, because Vercel's Python
# builder reads that file and cannot follow pip includes. This file points at
# it so there is only ever one list to keep pinned.
#
# The path is resolved relative to this file, so `pip install -r
# backend/requirements.txt` works from any directory.
-r ../requirements.txt
